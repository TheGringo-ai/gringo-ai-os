# Core dependencies
streamlit>=1.28.0
pandas>=1.5.0
psutil>=5.9.0
requests>=2.28.0

# Optional AI dependencies (comment out if not using)
# ollama (install separately via: curl -fsSL https://ollama.ai/install.sh | sh)

# Development dependencies (uncomment for development)
# pytest>=7.0.0
# black>=22.0.0
# isort>=5.10.0
# flake8>=5.0.0
